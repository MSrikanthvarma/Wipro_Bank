package org.scala.PartialFunction;

import static org.junit.Assert.*;

import org.junit.Test;

public class SquareRootTest {

	@Test
	public void testSquareRoot() {
		assertTrue(SquareRoot.squareRoot().isDefinedAt(10));
		assertTrue(!SquareRoot.squareRoot().isDefinedAt(-10));
	}
}
